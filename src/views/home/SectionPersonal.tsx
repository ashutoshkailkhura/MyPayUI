import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

export default function SectionPersonal({navigation}) {
  return (
    <View>
      <Text>SectionPersonal</Text>
    </View>
  );
}

const styles = StyleSheet.create({});
